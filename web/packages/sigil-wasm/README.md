# web/packages/sigil-wasm

Reserved. Loader package for `libsigil` compiled to WebAssembly, consumed by the
webapp and the browser extension. **Deferred** — depends on the wasm bindings
(`libsigil/bindings/wasm/`), not generated yet.
