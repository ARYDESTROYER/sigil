# web/apps/webapp

Reserved. The in-browser web app (a thin client over `libsigil-wasm`).
**Deferred** — depends on a real `libsigil` wasm artifact that does not exist
yet; a non-functional crypto demo on a security product is worse than none.
